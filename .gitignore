__pycache__/
*.pyc
venv/
.venv/
config.yaml
# data는 통째로 제외하되 리플레이 회귀 자산만 예외 (디렉토리째 제외하면
# 하위 재포함이 불가능해 data/* 패턴을 쓴다)
data/*
!data/replays/
data/replays/*
!data/replays/*.jsonl.gz
audio_cache/
dist/
build/
*.spec.bak

# C# 빌드 산출물
simhub/**/bin/
simhub/**/obj/
*.user
simhub/verify/stubout/
# Kokoro 등 로컬 TTS 모델 (수백 MB — 도구가 자동 다운로드)
models/
